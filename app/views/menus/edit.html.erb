<div class="container">
  <div class="row">
    <div>
      <h4>トレーニング名</h4>
      <%= form_with model: @menu, url: menu_path, local: true do |f| %>
      <div class="form-group">
        <%= f.label :メニュー名 %>
        <%= f.text_field :menu_name %>
        <%= f.submit "変更を保存", class: "btn btn-success" %>
      </div>
      <% end %>
    </div>
  </div>
</div>